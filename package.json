{
  "name": "framework-google-functions-framework",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "functions-framework --target=inngest --port=$PORT --source=dist",
    "dev": "npm run build && functions-framework --target=inngest --port=8080 --source=dist"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.0",
    "express": "^4.19.2",
    "inngest": "^3.6.2"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
